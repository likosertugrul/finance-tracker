# React Native / Expo, düz (hoisted) node_modules bekler — pnpm'in varsayılan
# izole/symlink yapısı Metro & native modül çözümlemesini bozar. Bu ayar pnpm'i
# npm benzeri düz bir node_modules üretmeye zorlar (RN monorepo standardı).
node-linker=hoisted
